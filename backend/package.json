{
  "name": "hospital-rag-backend",
  "version": "1.0.0",
  "description": "Backend for Hospital Management RAG Q&A Chatbot",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "node src/migrate.js",
    "seed": "node src/ingest.js",
    "seed:patients": "node src/seedPatients.js",
    "setup": "npm install && npm run migrate && npm run seed:patients && npm run seed"
  },
  "dependencies": {
    "@google/genai": "^2.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "multer": "^2.0.1",
    "pdf-parse": "^1.1.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
